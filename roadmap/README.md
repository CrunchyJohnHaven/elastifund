# Roadmap Route

| Field | Value |
|---|---|
| Route | `/roadmap/` |
| Entry file | `roadmap/index.html` |
| Status | `preview_stub` |
| Purpose | Public-safe checkpoint and forecast surface |
| Shared assets | `/site.css`, `/site.js` |

## Scope

This route is intentionally a stub. It summarizes checkpoints and confidence without presenting forecasts as guarantees.

## Naming And Layout Rules

- Keep the route entrypoint as `index.html`.
- Keep checkpoint confidence explicit and date-bounded.
- Keep shared styling and runtime data binding in root `/site.css` and `/site.js`.
