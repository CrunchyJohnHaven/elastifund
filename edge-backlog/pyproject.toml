[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "edge-backlog"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
edge-backlog = "edge_backlog.cli:main"
