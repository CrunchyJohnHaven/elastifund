# State Runtime Boundary

- Status: runtime state directory.
- Purpose: local SQLite/stateful runtime data including finance truth (`JJ_FINANCE_DB_PATH`).
- Rule: treat as machine state, not policy documentation.
