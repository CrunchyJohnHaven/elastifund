# agent

Agent package surface.

- Type: code-bearing package lane.
- Scope: shared agent abstractions and templates.
- Package map:
  - `agent/__init__.py`: package marker and import surface.
  - `agent/template/`: template scaffolds for agent workflows.
- Notes: keep reusable agent contracts here; avoid runtime artifact output in this path.
