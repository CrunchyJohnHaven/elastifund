# CODEX PLANNING PROMPT — Elastifund Parallel Instance Generator

> **Purpose:** Paste this prompt verbatim into Codex Planning. It will return a full execution plan organized by parallel instance. You then paste each instance block into a separate Codex session: "Execute Instance #N — [block]".
>
> **Evergreen:** This prompt reads live repo state. No hardcoded dates or balances. The instances discover current state at runtime.

---

## SYSTEM CONTEXT FOR CODEX PLANNING

You are the planning layer for Elastifund, an AI-run prediction market trading fund. Your job is to produce a parallel execution plan split across 6 independent Codex instances. Each instance runs autonomously with zero human intervention.

**Read these files first to discover current state (do not assume — read):**

```
CLAUDE.md                        → Operating model, current capital, cycle state
PROJECT_INSTRUCTIONS.md          → Priority queue, signal architecture, strategy status
COMMAND_NODE.md                   → Full project context
docs/REPO_MAP.md                  → Directory ownership and task routing
research/edge_backlog_ranked.md   → Ranked strategy backlog (131+ tracked)
FAST_TRADE_EDGE_ANALYSIS.md       → Latest pipeline results
REPLIT_NEXT_BUILD.md              → Website build spec
research/velocity_maker_strategy.md → Capital velocity benchmarks
```

After reading, produce the execution plan below. Each instance gets a self-contained block with: objective, files it owns, files it must not touch, exact steps, verification command, and handoff artifact.

---

## THE 6 PARALLEL INSTANCES

---

### INSTANCE #1 — EDGE SCANNER & TRADE DEPLOYMENT

**Objective:** Find the highest-edge, shortest-duration (<24h resolution) opportunities available right now on Polymarket and Kalshi. Deploy capital to the best ones within existing risk parameters.

**You own:** `bot/`, `execution/`, `strategies/`, `signals/`
**Do not touch:** `docs/`, `research/`, `deploy/`, website files

**Steps:**

1. Read `PROJECT_INSTRUCTIONS.md` Section 3 for current signal architecture and which signals are DEPLOYED vs BUILDING vs GATED.
2. Read `CLAUDE.md` "Current State" section for exact capital, position limits, daily loss cap, Kelly fraction, and execution mode.
3. Read `research/edge_backlog_ranked.md` for the full ranked backlog. Identify all strategies tagged as ready-to-deploy or building that target <24h markets.
4. Read `research/velocity_maker_strategy.md` for the proven velocity edge (72% win rate on <24h, 6007% ARR maker-only).
5. Pull current Polymarket markets via Gamma API: `GET https://gamma-api.polymarket.com/events?closed=false&limit=100`. Filter for `end_date` within 24 hours of now. Rank by liquidity and spread.
6. For Kalshi: check `bot/cross_platform_arb.py` for the matching logic. Pull available markets via the Kalshi API if credentials exist in `.env`.
7. Run the A-6 guaranteed-dollar scanner (`bot/constraint_arb_engine.py` → `bot/a6_sum_scanner.py`) against the neg-risk universe. Log any combo where cheapest construction < $0.95.
8. Run the VPIN/OFI toxicity check (`bot/ws_trade_stream.py` → `bot/vpin_toxicity.py`) on the top 10 most liquid <24h markets. Flag markets with VPIN > 0.7 as toxic — skip them.
9. For every viable opportunity: compute Kelly size (quarter-Kelly, capped at position limit from config), verify maker-only execution path, verify kill rules pass (`bot/kill_rules.py`).
10. If `jj-live.service` is STOPPED and you find ≥3 viable <24h opportunities with positive expected value after fees: prepare a restart recommendation with the specific markets, sizes, and expected hold times. Write to `reports/edge_scan_<timestamp>.json`.
11. If service is RUNNING: feed opportunities directly into the signal pipeline and let the orchestrator (`bot/jj_live.py`) handle execution.

**Verification:** `make test && python3 -c "from bot.jj_live import TradingBot; print('import ok')"`

**Handoff artifact:** `reports/edge_scan_<timestamp>.json` with fields: `{markets: [{id, title, resolution_time, edge_pct, kelly_size, signal_sources, kill_rule_status}], capital_available, recommended_action}`

---

### INSTANCE #2 — REPLIT WEBSITE BUILD

**Objective:** Improve the Replit website (https://elastifund.replit.app) with fresh data, corrected metrics, and any new content generated by other instances in this cycle.

**You own:** Website source files (Replit project), `REPLIT_NEXT_BUILD.md`
**Do not touch:** `bot/`, `execution/`, `strategies/`, `signals/`

**Steps:**

1. Read `REPLIT_NEXT_BUILD.md` for the full build spec, including what the current site does well, what it lacks, and the target architecture.
2. Read `CLAUDE.md` "Current State" for live capital, cycle number, strategy counts, test counts, dispatch counts — these are the numbers that must be updated on the site.
3. Read `README.md` for the public-facing metrics that must match the website.
4. Read `FAST_TRADE_EDGE_ANALYSIS.md` for the latest pipeline results to surface.
5. Read `research/edge_backlog_ranked.md` for updated strategy counts by status (deployed, building, rejected, etc.).
6. Read the latest `docs/diary/` entry for new build diary content.
7. Correct all stale metrics flagged in `REPLIT_NEXT_BUILD.md` "What the current site lacks" section: server location, cost, win rate, daily loss limit.
8. Update the scorecard numbers: strategy count, test count, dispatch count, signal source count, competitor count.
9. Add any new strategy encyclopedia entries (check for new D-*, B-*, SA-*, RE-* or R-* entries that aren't on the site yet).
10. Add the latest build diary entry if one exists that isn't published.
11. If Instance #1 produced `reports/edge_scan_<timestamp>.json`, surface the opportunity count and market selection summary (no specific trade details — public site).
12. Add a "System Status" indicator showing whether `jj-live.service` is running or paused and why.
13. Ensure the site footer timestamp updates to now.

**Verification:** Site loads without console errors. All metric values match `CLAUDE.md` current state. No broken links.

**Handoff artifact:** Commit message listing every metric changed and every section added.

---

### INSTANCE #3 — COMMAND NODE & DOCUMENTATION SYNC

**Objective:** Update `COMMAND_NODE.md`, `PROJECT_INSTRUCTIONS.md`, and all canonical docs to reflect the current cycle's state. This is the source of truth that every other instance reads.

**You own:** `COMMAND_NODE.md`, `PROJECT_INSTRUCTIONS.md`, `docs/`, `research/`, `README.md`, `AGENTS.md`
**Do not touch:** `bot/`, `execution/`, `strategies/`, `signals/`, website files

**Steps:**

1. Read `CLAUDE.md` "Current State" section as the ground truth baseline.
2. Read every `reports/*.json` file modified in the last 24 hours to find new results.
3. Read `FAST_TRADE_EDGE_ANALYSIS.md` for latest pipeline outputs.
4. Count passing tests: run `make test 2>&1 | tail -5` and `make test-polymarket 2>&1 | tail -5`. Record exact numbers.
5. Count dispatches: `ls research/dispatches/ | wc -l`.
6. Count strategies by status in `research/edge_backlog_ranked.md`.
7. Update `COMMAND_NODE.md` with: current capital, current cycle, strategy counts, test counts, dispatch counts, service status, last action taken, next action planned.
8. Update `PROJECT_INSTRUCTIONS.md` Section 1 status paragraph and Section 3 signal status tags (DEPLOYED / BUILDING / GATED / etc.).
9. Update `README.md` scorecard if any headline numbers changed.
10. Update `CLAUDE.md` "Current State" section date, cycle, capital, test counts, dispatch counts, and next action.
11. If Instance #1 produced edge scan results, update `research/edge_backlog_ranked.md` with any new status changes.
12. Run `make hygiene` to verify doc consistency.

**Verification:** `make hygiene && make verify`

**Handoff artifact:** Diff summary: which files changed, which numbers moved, what the new "next action" is.

---

### INSTANCE #4 — AWS VPS DEPLOYMENT & INFRASTRUCTURE

**Objective:** Push all code changes to the Dublin VPS (AWS Lightsail eu-west-1). Ensure the service is in the correct state (running or stopped) based on Instance #1's recommendation.

**You own:** `deploy/`, `config/`, `.github/workflows/`, VPS operations
**Do not touch:** `docs/`, `research/`, website files

**Steps:**

1. Read `CLAUDE.md` "Current State" for current VPS status, service state, and deployment config.
2. Read `deploy/` directory for deployment scripts, systemd service files, and environment templates.
3. Read `.github/workflows/` for CI/CD pipeline definitions.
4. Run the full test suite locally: `make verify`. Do not deploy if tests fail.
5. If tests pass: push all committed changes to the VPS via the deployment mechanism defined in `deploy/`.
6. Verify the deployed code matches the repo HEAD: compare git SHA on VPS vs local.
7. Check `.env` on VPS has all required keys (list from `.env.example`). Do not create or modify API keys — flag missing ones for human action.
8. If Instance #1's handoff recommends restarting `jj-live.service`: verify the restart conditions are met (≥3 viable opportunities, all kill rules configured, capital confirmed). If conditions met, prepare the restart command but DO NOT EXECUTE unless current state is already RUNNING. If service is STOPPED, flag as "restart-recommended" in handoff — this is a human escalation per CLAUDE.md rules (spending real money).
9. Verify logging pipeline: ensure `bot/elastic_client.py` can reach Elasticsearch (or gracefully degrades).
10. Verify systemd service file is current with latest bot arguments and environment.
11. Run a dry-run cycle if service is STOPPED: `python3 bot/jj_live.py --dry-run --cycles=1` on VPS to confirm runtime health.

**Verification:** VPS git SHA matches repo HEAD. `systemctl status jj-live.service` returns expected state. Dry-run completes without error.

**Handoff artifact:** `reports/deploy_<timestamp>.json` with fields: `{vps_sha, repo_sha, service_status, dry_run_result, missing_env_keys, restart_recommended}`

---

### INSTANCE #5 — GITHUB SYNC & IMPROVEMENT VELOCITY CHARTS

**Objective:** Push all changes to GitHub. Generate and commit updated improvement velocity charts and ARR estimates at repo root. Cover both the trading agent system and the non-trading agent system.

**You own:** `.github/`, root chart files, `README.md` (chart section only), `nontrading/`
**Do not touch:** `bot/` internals, `docs/` prose, website files

**Steps:**

1. Read `CLAUDE.md` "Current State" for cycle number, dates, capital, and strategy counts.
2. Read `research/velocity_maker_strategy.md` for the proven velocity benchmarks (72% win rate, 6007% ARR maker-only on <24h).
3. Read `nontrading/` directory for non-trading revenue lane status, modules, and any revenue metrics.
4. Read all `docs/diary/` entries to extract a timeline of: cycle dates, strategies tested, strategies deployed, capital changes, test count growth, dispatch count growth.
5. Read `research/edge_backlog_ranked.md` for strategy funnel metrics: total tracked → research pipeline → building → deployed → rejected.
6. Generate `improvement_velocity.json` at repo root with structure:
   ```json
   {
     "generated_at": "<ISO timestamp>",
     "cycle": "<current cycle>",
     "trading_agent": {
       "strategies_total": N,
       "strategies_deployed": N,
       "strategies_building": N,
       "strategies_rejected": N,
       "test_count": N,
       "dispatch_count": N,
       "capital_deployed": N,
       "backtest_win_rate": N,
       "velocity_arr_estimate": N,
       "maker_excess_return_pct": N
     },
     "non_trading_agent": {
       "modules_complete": N,
       "revenue_lanes_active": N,
       "compliance_status": "<status>"
     },
     "velocity_metrics": {
       "strategies_per_cycle": N,
       "code_modules_per_cycle": N,
       "tests_added_per_cycle": N,
       "dispatches_per_cycle": N,
       "cycle_duration_days": N
     },
     "arr_estimate": {
       "trading_arr": N,
       "non_trading_arr": N,
       "combined_arr": N,
       "methodology": "Trading: velocity_maker backtest × current capital × Kelly fraction. Non-trading: compliance revenue pipeline × conversion estimate."
     }
   }
   ```
7. Generate `improvement_velocity.svg` (or `.png`) chart at repo root using matplotlib or mermaid showing:
   - X-axis: cycle/date, Y-axis: cumulative strategies tested, deployed, and rejected (stacked area)
   - Secondary Y-axis: ARR estimate trend line
   - Annotations for key events (first trade, strategy pivots, platform additions)
8. Generate `arr_estimate.svg` at repo root showing:
   - Trading ARR projection based on current capital × velocity win rate × annualized turnover
   - Non-trading ARR projection based on compliance pipeline
   - Combined ARR with confidence interval
9. Update `README.md` to embed or link the charts (image references only — don't bloat the README).
10. Stage all changes. Commit with message: `chore: update velocity charts and ARR estimates [Cycle N]`.
11. Push to `main` (or current branch).
12. Verify GitHub Actions CI passes after push.

**Verification:** `git log -1 --oneline` shows the new commit. Charts render correctly in GitHub markdown. CI green.

**Handoff artifact:** Git commit SHA + chart file paths.

---

### INSTANCE #6 — DATA PULL & PIPELINE REFRESH

**Objective:** Pull fresh market data, run the edge discovery pipeline, refresh calibration, and produce updated `FAST_TRADE_EDGE_ANALYSIS.md`.

**You own:** `src/`, `backtest/`, `data/`, `data_layer/`, `reports/`
**Do not touch:** `bot/` (except reading signal configs), `docs/`, website files

**Steps:**

1. Read `PROJECT_INSTRUCTIONS.md` Section 3 for current signal architecture and data sources.
2. Read `docs/strategy/edge_discovery_system.md` for the hypothesis testing pipeline architecture.
3. Read `src/data_pipeline.py` for the data ingestion logic and API endpoints.
4. Pull fresh Polymarket data:
   - Active markets: `GET https://gamma-api.polymarket.com/events?closed=false&limit=500`
   - Recent trades: `GET https://data-api.polymarket.com/trades?limit=1000`
   - Order book snapshots for top liquid markets
5. Pull fresh Kalshi data if API credentials exist in `.env`.
6. Store raw pulls in `data/` with timestamp directories.
7. Run feature engineering: `python3 src/feature_engineering.py` on the fresh data.
8. Run the backtest suite on all deployed strategies against the fresh data: `python3 src/backtest.py --strategies=deployed`.
9. Run confidence calibration: `python3 src/confidence_calibration.py` to update Platt scaling parameters.
10. Run the hypothesis explorer: `python3 src/hypothesis_explorer.py` to check if any backlog strategies have become viable with new data.
11. Generate updated `FAST_TRADE_EDGE_ANALYSIS.md` via `python3 src/reporting.py`.
12. If any new edges are discovered (strategies moving from research → viable), append them to `research/edge_backlog_ranked.md` with evidence.
13. Run `make test` to verify nothing broke.

**Verification:** `make test` passes. `FAST_TRADE_EDGE_ANALYSIS.md` timestamp is current. `data/` has fresh timestamped directory.

**Handoff artifact:** `FAST_TRADE_EDGE_ANALYSIS.md` (updated) + `reports/pipeline_<timestamp>.json` with: `{markets_scanned, edges_found, calibration_drift, new_viable_strategies}`

---

## INSTANCE DEPENDENCY MAP

```
Instance #6 (Data Pull) ──────┐
                               ├──→ Instance #1 (Edge Scanner) ──→ Instance #4 (VPS Deploy)
Instance #3 (Command Node) ───┘                                        │
       │                                                               │
       └──→ Instance #2 (Website) ←────────────────────────────────────┘
                                                                       │
Instance #5 (GitHub + Charts) ←────────────────────────────────────────┘
```

**If running all 6 truly in parallel:** Instances read stale state on first pass. That's fine. The flywheel is designed for eventual consistency — each cycle corrects the previous cycle's drift. After 2-3 passes, all instances converge.

**If you want strict ordering:** Run #6 and #3 first, then #1 and #2, then #4 and #5.

---

## ESCALATION RULES (EVERY INSTANCE FOLLOWS THESE)

Escalate to human (John) ONLY when:
- Spending real money (switching paper → live, restarting stopped service)
- Changing risk parameters (position sizes, loss limits, Kelly fractions)
- Architectural decisions with no clear best option
- Something is broken after exhausting debugging
- Legal/compliance questions

For everything else: execute, commit, push, report.

---

## HANDOFF CONTRACT (EVERY INSTANCE PRODUCES THIS)

```
INSTANCE #N HANDOFF
---
Files changed: [list]
Commands run: [list]
Key findings: [1-3 sentences]
Numbers that moved: [list with before→after]
Unverified: [anything the next cycle should check]
Next instance can edit these files: [yes/no per file]
```

---

## HOW TO USE THIS PROMPT

1. Paste this entire document into Codex Planning.
2. Codex Planning reads the repo files listed in "SYSTEM CONTEXT" and fills in current numbers.
3. Codex Planning returns 6 instance blocks with current state injected.
4. You paste each block into a separate Codex session: "Execute Instance #1 — [block]"
5. Instances run in parallel. Collect handoff artifacts. Repeat next cycle.

The system improves every pass. The velocity charts prove it.
